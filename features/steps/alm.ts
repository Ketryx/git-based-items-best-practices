import { Given } from "@cucumber/cucumber";
import { expect } from "@playwright/test";

Given("I have a project with a feature file", () => {
  expect(true);
});

Given("I run the feature file", () => {
  expect(true);
});

Given("I should see the results", () => {
  expect(true).toBe(true);
});
